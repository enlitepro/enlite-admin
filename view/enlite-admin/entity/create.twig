{% extends "enlite-admin/layout/layout" %}

{% block content %}

    <div class="page-header">
        <h1>Add "{{ entity.name }}"</h1>
    </div>

    {% do form.setAttribute('action', url('admin/entity/entity/create', {'entity': entity.name})) %}
    {{ form(form) }}

{% endblock %}
